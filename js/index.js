$(window).on('load', function() { // makes sure the whole site is loaded
$('.preloader').fadeOut(1000); // will first fade out the loading animation
$('.content').fadeIn(1000); // will fade out the white DIV that covers the website.
})
